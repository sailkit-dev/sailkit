{
  "name": "bearing-dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "turbo": "^2.7.0",
    "typescript": "^5.3.0"
  }
}
